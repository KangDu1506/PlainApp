<Window x:Class="PlainApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlainApp"
        xmlns:partials="clr-namespace:PlainApp.Partials"
        xmlns:views="clr-namespace:PlainApp.Views"
        mc:Ignorable="d" 
        d:DesignHeight="600" d:DesignWidth="800"
        Style="{StaticResource {x:Type Window}}"
        WindowState="Maximized"
        Title="MainWindow">

    <Grid x:Name="RootGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <partials:SideMenu Grid.ColumnSpan="2"
                           Panel.ZIndex="999"
                           x:Name="MainSideMenu"/>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <partials:Header Grid.Row="0"
                             x:Name="MainHeader"/>
            <views:HomeView Grid.Row="1"
                            x:Name="MainHomeView"/>
        </Grid>
    </Grid>
</Window>
