<UserControl x:Class="PlainApp.Partials.Node"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlainApp.Partials"
             xmlns:lang="clr-namespace:PlainApp.Languages"
             xmlns:services="clr-namespace:PlainApp.Services"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="400">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/Partials/NodeStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Border Padding="5"
            Style="{StaticResource NodeStyle}"
            Margin="10">
        <Grid Style="{StaticResource NodeGridStyle}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border BorderThickness="0"
                    CornerRadius="2.5"
                    Background="{DynamicResource 70%TransparentBlackBrush}"
                    Grid.Column="0" Grid.Row="0"
                    ClipToBounds="True">
                <TextBlock x:Name="NodeIndexNumber" 
                           Style="{StaticResource NodeIndexNumberStyle}"/>
            </Border>
            <TextBlock x:Name="NodeTitle"
                       Style="{StaticResource NodeTitleStyle}"
                       Grid.Column="1" Grid.Row="0"/>
            <Border Grid.Column="2" Grid.Row="0"
                    CornerRadius="2.5"
                    VerticalAlignment="Center"
                    Background="{DynamicResource 70%TransparentBlackBrush}">
                <Button x:Name="CompleteCheckButton"
                        Style="{StaticResource CompleteCheckButtonStyle}"                        
                        Content="{Binding Source={x:Static services:LanguageManager.Instance}, Path=[NodeCompleteButton]}"
                        />
            </Border>

            <Border Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2"
                    >
                <TextBlock x:Name="NodeDueDate"
                           Style="{StaticResource NodeDueDateStyle}"/>
            </Border>

            <TextBlock x:Name="NodeProgress"
                       Style="{StaticResource NodeProgressStyle}"
                       Grid.Column="2" Grid.Row="1"/>

            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3">
                <TextBox x:Name="NodeDescription" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
