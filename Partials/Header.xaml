<UserControl x:Class="PlainApp.Partials.Header"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlainApp.Partials"
             xmlns:lang="clr-namespace:PlainApp.Languages"
             xmlns:services="clr-namespace:PlainApp.Services"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/Partials/HeaderStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border Height="100"
            Margin="10 25"
            Background="{DynamicResource 70%TransparentBlackBrush}"
            CornerRadius="10">
        <Grid Height="80">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       x:Name="WindowTitle"
                       Style="{StaticResource AppHeaderTextBlock}"/>

            <Grid Grid.Column="2"
                  HorizontalAlignment="Right">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox x:Name="ThemeSelectorComboBox"
                          Grid.Column="0"
                          Style="{StaticResource SelectorComboBoxStyle}"
                          Width="100"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Center"
                          SelectionChanged="ThemeSelectorComboBox_SelectionChanged">
                    <ComboBoxItem Content="{Binding Source={x:Static services:LanguageManager.Instance}, Path=[ThemeSelect_Light]}" Tag="Light"/>
                    <ComboBoxItem Content="{Binding Source={x:Static services:LanguageManager.Instance}, Path=[ThemeSelect_Dark]}" Tag="Dark"/>
                </ComboBox>

                <ComboBox x:Name="LanguageSelectorComboBox"
                          Grid.Column="1"
                          Style="{StaticResource SelectorComboBoxStyle}"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Center"
                          Width="150"
                          Margin="20 0"
                          SelectionChanged="LanguageSelectorComboBox_SelectionChanged">
                    <ComboBoxItem Content="English" Tag="en-US"/>
                    <ComboBoxItem Content="日本語" Tag="ja-JP"/>
                    <ComboBoxItem Content="Tiếng Việt" Tag="vi-VN"/>
                </ComboBox>
            </Grid>
        </Grid>
    </Border>
</UserControl>
